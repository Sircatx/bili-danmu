# Bilibili 弹幕云端采集 — 交接文档

> 更新时间：2026-07-21（第四轮：✅ 部署成功！SSH 进主机 + Docker 直接跑 + systemd 自启）
> 状态：**🎉 完成！弹幕采集已在 HA 主机以 Docker 容器 + systemd 服务 7×24 运行，房间 5540720，弹幕按天落 /share/danmu（Samba 可见）。崩溃/断电自动重启已验证。**
>
> **本轮完成：**
> - ✅ 修复 `danmu.py` 两个 bug：① `github_remote()` 之前是字面量 `***@` 从没嵌入真实 token（私有仓库必然认证失败）；② `git_setup()` 用 `git clone` 到已存在的 `data/` 目录会报 "destination not empty"，导致容器重启续传逻辑失效——改为幂等的 `git init + remote + fetch + checkout`。
> - ✅ 建好两个仓库：`Sircatx/bili-danmu`（公开·代码）、`Sircatx/bili-danmu-logs`（**私有**·日志）。
> - ✅ 代码已推送到 `bili-danmu`。
> - ✅ **真实端到端验证**：游客模式连房间 `1742802667` → 抓到真实弹幕（如"中午了""准备下雨了"）→ 每 15s git push → 确认弹幕日志文件真的出现在私有仓库。测试日志已清理。
> - ⚠️ **纠正旧文档**：弹幕日志落在**仓库根目录** `danmu_<房间号>.log`（不是旧文档写的 `data/danmu_6.log`），因为 `WORKDIR` 本身就是 git 仓库根。

---

## 一、项目背景与目标

- **原始需求**：把 GitHub 上的 [BanqiJane/Bilibili_Danmuji](https://github.com/BanqiJane/Bilibili_Danmuji)（Java + Spring Boot 完整弹幕姬）挂到云端，**不花钱**。
- **需求收窄**：因为免费云都要绑信用卡（见第三节），用户中途改为「只要一个**能抓取某直播间弹幕**的小工具挂在云端即可」，不一定要跑那个完整 Java 程序。
- **最终目标**：云端 24/7 抓取指定直播间弹幕，弹幕日志能换电脑查看，零成本、免绑卡。

---

## 二、关键决策与放弃的方案（含原因）

| 方案 | 结论 | 原因 |
|---|---|---|
| Oracle Cloud Always Free（2 核 12G 永久） | ❌ 放弃 | 必须绑信用卡验证身份，用户不愿意 |
| Fly.io | ❌ 放弃 | ① 也要绑信用卡；② 已取消永久免费（仅 7 天试用）；③ 脚本还踩了 `fly apps status` 不存在子命令的坑 |
| Hugging Face Spaces | ⚠️ 备选 | 100% 免绑卡，但 48 小时无访问会休眠，需 UptimeRobot 定时 ping；磁盘临时、日志须外发 |
| **Koyeb + GitHub** | ✅ **采用** | 免费 nano 实例（512MB / 0.1vCPU）**免绑卡、永不休眠**；从 GitHub 仓库直接拉 Dockerfile 部署，GitHub 同时充当代码源和日志仓库 |

**最终落地形态**：用 `bilibili-api-python` 写的轻量 Python 脚本（非原 Java 程序），以 **Koyeb Worker** 常驻跑，**游客模式**连 B 站（无需登录扫码），弹幕定时 `git push` 到 GitHub 私有仓库。

---

## 三、当前方案架构

```
Koyeb Worker (nano, 免绑卡, 永不休眠)
  └─ 运行 danmu.py
       ├─ 游客模式连接 B 站直播间 WebSocket，实时收弹幕/礼物/上舰
       ├─ 弹幕写入本地 data/danmu_<房间号>.log
       ├─ 每 PUSH_INTERVAL 秒 git push → GitHub 私有日志仓库 (bili-danmu-logs)
       └─ 监听 PORT(7860) 做 HTTP 健康检查（worker 不暴露端口，起一个无害）

换电脑查看：git clone 私有日志仓库 → 打开 data/danmu_<房间号>.log
实时查看：Koyeb 服务 Logs 标签页
```

---

## 四、待办（部署步骤，接手人/用户执行）

> 这一步**还没做**，GitHub 仓库和 Koyeb 服务都还没建。

1. **GitHub 建两个仓库（必须分开）**
   - `bili-danmu`：放代码（`danmu.py` / `Dockerfile` / `requirements.txt`）
   - `bili-danmu-logs`：**私有**，只存弹幕日志
   - ⚠️ 不分开会导致弹幕每 5 分钟 push 触发 Koyeb 反复重新部署

2. **生成 GitHub 令牌**（classic，勾 `repo` 权限），复制保存（只显示一次）

3. **推送代码到 `bili-danmu`**
   ```powershell
   cd C:\Project\bilibili-danmu-capture
   git init -q 2>$null
   git remote add origin https://github.com/你的名/bili-danmu.git
   git add danmu.py Dockerfile requirements.txt
   git commit -m "deploy danmu capture"
   git push -u origin main
   ```

4. **Koyeb 控制台部署**
   - 连 GitHub → 选 `bili-danmu` → Builder 选 **Dockerfile** → Instance **nano** → Service type **Worker** → Scaling **Min instances = 1**
   - 环境变量：
     | 变量 | 值 | 备注 |
     |---|---|---|
     | `ROOM_ID` | `6` | 要抓的房间号 |
     | `GITHUB_TOKEN` | 令牌 | 存为 **Secret**（🔒） |
     | `GITHUB_REPO` | `你的名/bili-danmu-logs` | 日志仓库 |
     | `GITHUB_BRANCH` | `main` | |
     | `PUSH_INTERVAL` | `300` | 推送间隔秒，想少丢可改 120 |
   - 点 **Deploy**

5. **验证**：Koyeb Logs 搜 `已推送弹幕到 GitHub`；GitHub 日志仓库出现 `data/danmu_6.log` 即成功。

---

## 五、文件清单与位置

| 文件 | 说明 | 状态 |
|---|---|---|
| `C:\Project\bilibili-danmu-capture\danmu.py` | 云端版弹幕采集主脚本 | ✅ 完成+本地验证 |
| `C:\Project\bilibili-danmu-capture\Dockerfile` | 容器构建（python:3.12-slim + git） | ✅ 完成 |
| `C:\Project\bilibili-danmu-capture\requirements.txt` | 依赖：bilibili-api-python / aiohttp / websocket-client | ✅ 完成 |
| `C:\Project\bilibili-danmu-capture\README.md` | 本地运行用法 | ✅ 完成 |
| `C:\Project\bilibili-danmu-capture\README_deploy.md` | Koyeb 部署详细文档 | ✅ 完成 |
| `C:\Project\bilibili-danmu-capture\venv\` | 本机 Python venv，依赖已装，可本地跑 | ✅ 完成 |
| `C:\Project\bilibili-danmu-capture\交接文档.md` | 本文档 | ✅ 完成 |
| `C:\Project\Bilibili_Danmuji-cloud\` | **已废弃**的 Fly.io 方案（deploy.bat/access.bat/danmuji.ps1/Dockerfile/fly.toml/README.md） | ❌ 不用，可删除 |

> 本地跑法（可选）：`cd C:\Project\bilibili-danmu-capture` → `.\venv\Scripts\python danmu.py 房间号`

---

## 六、脚本配置项（环境变量参考）

| 变量 | 默认 | 说明 |
|---|---|---|
| `ROOM_ID` | `6` | 直播间房间号（也可命令行参数传） |
| `PORT` | `7860` | HTTP 健康检查端口 |
| `WORKDIR` | `data` | 弹幕本地暂存目录 |
| `GITHUB_TOKEN` | 空 | GitHub 私人令牌（repo 权限），建议 Koyeb Secret |
| `GITHUB_REPO` | 空 | 日志仓库 `owner/repo` |
| `GITHUB_BRANCH` | `main` | 日志仓库分支 |
| `PUSH_INTERVAL` | `300` | 多少秒推送一次 |

未配置 `GITHUB_REPO`/`GITHUB_TOKEN` 时，脚本仅本地记录弹幕（容器重启会丢），不崩溃。

---

## 七、已验证（本地实测通过）

- ✅ Python 语法编译通过（`py_compile`）
- ✅ `bilibili_api` 可正常导入
- ✅ HTTP 健康检查端口 7860 正常响应（`danmu capture running, room=6`）
- ✅ **B 站游客模式连接认证成功**（15 秒测试连上 `wss://...chat.bilibili.com` 并认证，确认云端能真抓弹幕，无需登录）
- ✅ 无 GitHub 变量时正确跳过推送逻辑，不报错

---

## 八、注意事项 / 坑

1. **Koyeb 免费实例磁盘是临时的**，容器重启/重建本地 `data/` 会丢——但弹幕已 push 到 GitHub，启动会先 `pull` 续传，不丢。
2. 若 Koyeb 在两次 push 之间重启，那一个间隔内的弹幕会丢；把 `PUSH_INTERVAL` 调小（如 120）可减少损失。
3. 若 Koyeb 提示需要绑卡才能部署，说明你地区免费档需验证，退回 **HuggingFace Spaces**（README 旧方案思路）或 Oracle（绑卡）。
4. 弹幕是私人数据，**日志仓库务必设私有**。
5. `GITHUB_TOKEN` 务必存成 Koyeb **Secret**，别用普通变量（会进日志明文）。

---

## 九、账号与凭证状态

| 项目 | 状态 |
|---|---|
| Koyeb 账号 | ✅ 已注册 |
| GitHub 账号 | ✅ 可用（Sircatx） |
| GitHub 代码仓库 `Sircatx/bili-danmu`（公开） | ✅ 已建 + 代码已推送 |
| GitHub 日志仓库 `Sircatx/bili-danmu-logs`（私有） | ✅ 已建（当前仅 README，等部署后自动写入弹幕） |
| GitHub Personal Access Token（repo 权限） | ✅ 已有（存于 Hermes memory，login=Sircatx，scope=repo） |
| Koyeb 服务已部署运行 | ❌ **未部署（唯一剩余步骤，需在 Koyeb 网页控制台点击完成）** |

---

## 十、环境清理记录

- 用户曾用 Fly.io 尝试，本机残留 `C:\Users\Sircatx\.fly`（Fly CLI 配置，约 109MB）。
- 已确认：`deploy.bat` 当年卡在登录/绑卡从未跑完，**没真正安装过 fly**；`.fly` 是用户此前自己就有的。
- 2026-07-21 用户要求删除，**已永久删除** `C:\Users\Sircatx\.fly`（回收站方式被本沙箱安全策略拦截，故走永久删除，不可从回收站恢复）。
- 已废弃的 Fly 方案文件仍在 `C:\Project\Bilibili_Danmuji-cloud\`，可随时删除，不影响当前 Koyeb 方案。

---

## 十一、第三轮进展（2026-07-21）：按天分类 + 移到 Home Assistant

### 已完成
- ✅ **弹幕按天分类**：`danmu.py` 改为 `WORKDIR/danmu_<房间号>/<YYYY-MM-DD>.log`，跨零点自动切文件；礼物/上舰也落盘。本地房间 **5540720** 实测通过。代码已推 `Sircatx/bili-danmu`。
- ✅ **平台重选**：Koyeb 被 Mistral 收购冻结部署入口；HF 免费档砍了 Python（Docker/Gradio Space 需 PRO）。最终选择跑在用户自己的 **Home Assistant**（24h 常驻、零成本）。
- ✅ **摸清 HA**：`192.168.1.3`，**HA Supervised**（非 HAOS），Core 2026.7.1 / Supervisor 2026.07.3 / Docker 24.0.7 / **arch=aarch64**（machine=qemuarm-64）/ OS=Debian 11。装了 Samba share（`\\192.168.1.3\addons` 与 `\share` 免密可写）。
- ✅ **做好本地 add-on 文件包**（`C:\Project\bilibili-danmu-capture\addon\`：config.yaml/build.yaml/Dockerfile/run.sh/danmu.py），已同步到 `\\192.168.1.3\addons\bili_danmu`。日志设计写到 `/share/danmu/`，Samba 直接看，不必依赖 GitHub。
- ✅ **打通 Supervisor API**：浏览器（Kimi WebBridge，session=ha，已登录管理员 Sircatx）里用 HA WebSocket `supervisor/api` 命令可 reload store / install / 读 info——绕开 ingress iframe。本地 add-on `local_bili_danmu` 已被 store 识别。
- ✅ **电脑代理中继**：`C:\Users\Sircatx\clash-lan-proxy.py` 已跑（`0.0.0.0:7898 → 127.0.0.1:7897`，Clash Verge mixed-port）。实测 LAN `192.168.1.4:7898` 可 CONNECT 翻墙（HTTP 200）。防火墙已放行。这是给 HA 走电脑代理用的。

### ⛔ 卡点：HA 加载项**构建管道系统级故障**
- 任何本地 add-on 安装都**稳定 15 秒失败**，报 `unknown error while trying to build the image`。
- **已排除网络**：连"最小 add-on"（仅 `FROM base + echo`，零 pip、零 COPY 依赖）都同样 15s 失败；且换用**本地已缓存**的 HA core 镜像 `ghcr.io/home-assistant/qemuarm-64-homeassistant:2026.7.1` 当 base **仍然** 15s 失败 → 与镜像拉取无关。
- **从电脑侧实测 ghcr.io 完全可达**（拿 token + 拉 manifest 均 200，1.3s），进一步佐证不是墙的问题。
- **真正病根**：`resolution/info` 报系统 `unsupported: [os, systemd_journal]`。**systemd-journal-gatewayd socket 缺失**（读 Supervisor 日志也因此报 `HostNotSupportedError`）。高度怀疑 Docker 构建日志无法写入 journal → 构建流程直接 abort。**这是主机 OS 层问题，隔着 HA API/Samba 修不了，必须上主机 shell。**

### 🔑 最终一步需要：SSH 到 Debian 主机
- **凭据坑**：用户给的 `home / 0216....`（4 个点是真字符）是 **HA 网页登录账号**（截图见"人员/用户"里改密码），**不是 Debian 系统账号**。用它 paramiko SSH 登录 `192.168.1.3:22` 失败（服务器允许 publickey+password，但该组合被拒）。试过 root/homeassistant/pi/debian + 该密码也都失败（已停止试探，避免锁号）。
- 22 端口开放且是标准 OpenSSH（ED25519）。需要**真正的系统用户名+密码**（或密钥）。
- **拿到系统 shell 后的两条最终方案（择一）**：
  1. **修构建**：给 Docker daemon 配代理走电脑中继 → `/etc/systemd/system/docker.service.d/http-proxy.conf` 写 `HTTP_PROXY=http://192.168.1.4:7898`（虽已证明非网络问题，但配上无害）；真正要修的是 systemd journal / 重装/修复 supervisor 构建环境。
  2. **绕开加载项，直接系统级跑**（更省事，推荐）：主机本就有 python3 + Docker。直接
     `pip3 install bilibili-api-python`（走 `HTTPS_PROXY=http://192.168.1.4:7898`）→ 把 `danmu.py` 放 `/share/` 或 `/home/home/` → 写一个 systemd unit（`ROOM_ID=5540720`、`WORKDIR=/share/danmu`）开机自启 + 崩溃重启。零 Docker 构建，零加载项，最干净。日志照样落 `/share/danmu/`，Samba 可看。

### 复现/操作备忘
- WebBridge：`~/.kimi-webbridge/bin/kimi-webbridge.exe status`；浏览器 session 名 `ha`，已登录 HA 管理员。
- Supervisor API 调用法：HA 前端 localStorage.hassTokens.access_token → WebSocket `ws://192.168.1.3:8123/api/websocket` 发 `{type:'supervisor/api', endpoint, method}`。token 约 30 分钟过期，过期就重新 navigate 刷新页面。
- add-on 源目录 `C:\Project\bilibili-danmu-capture\addon\`；同步脚本 `C:\tmp\copy_addon.ps1`（注意 bash 会吞 `\\`，UNC 路径要走 .ps1 文件）。
- 代理中继随时可重启：`python C:\Users\Sircatx\clash-lan-proxy.py`。

---

## 十二、✅ 第四轮：部署成功（2026-07-21 晚）

### 🔑 突破口：拿到主机 SSH（系统账号，非 HA 网页账号）
- **设备真相**：内核 `5.15.110-ophub` → 这是一台刷了 Armbian 的 **ARM 电视盒子/小主机**（板载 eMMC，aarch64，4 核，1.8G 内存，Debian 11，Python 3.9.2，Docker 24.0.7）。
- **系统账号**：`root` / `Aa112211.`（注意末尾有个点）。⚠️ 这跟 HA 网页账号（Sircatx/home）、Samba 账号（homeassistant）完全是三套东西，前两个都 SSH 登不进，只有这个能。
- SSH：`ssh root@192.168.1.3`（22 端口，OpenSSH，ED25519）。本机用 paramiko（`pip install paramiko`）连。

### 🧩 网络真相（推翻之前的猜测）
- **ghcr.io 主机裸连 OK**（所有 HA 官方容器都是从 ghcr 拉的）。
- **docker.io（Docker Hub）被墙**（`registry-1.docker.io` 裸连超时）。这才是官方加载项 core_ssh 76s 失败的原因（它某些层在 docker.io）。
- **清华 pypi、b站 API 主机直连都 OK**。
- 加载项 15s 失败的真凶仍是 `systemd_journal` 缺失导致 Supervisor 本地构建管道崩（与网络无关）——但我们**绕开了整个加载项体系**，所以不再受影响。

### 🚀 最终部署架构（Docker + systemd，不碰 HA 加载项）
1. **给 Docker daemon 配代理**（让主机能拉 docker.io 的 python 镜像）：
   `/etc/systemd/system/docker.service.d/http-proxy.conf` 写 `HTTP_PROXY/HTTPS_PROXY=http://192.168.1.4:7898`（电脑 Clash 中继），`systemctl daemon-reload && systemctl restart docker`。**注意**：这只影响 daemon 拉镜像，不影响容器运行时；采集容器 b站直连不走代理。
2. **代码上主机**：`danmu.py` → `/opt/danmu/danmu.py`（SFTP 传，含按天分类 + PORT=0 跳过健康检查）。
3. **构建镜像** `danmu:latest`（237MB）：`/opt/danmu/Dockerfile` = `FROM python:3.11-slim` + `pip install -i 清华源 bilibili-api-python aiohttp` + `COPY danmu.py`。⚠️ **关键坑**：新版 bilibili-api **必须**额外装 `aiohttp`（或 httpx/curl_cffi），否则报 `尚未安装第三方请求库`。
4. **systemd 服务** `/etc/systemd/system/danmu.service`：`docker run --rm --name danmu_5540720 -e ROOM_ID=5540720 -e PORT=0 -e WORKDIR=/data -v /usr/share/hassio/share/danmu:/data danmu:latest`，`Restart=always RestartSec=10`，`enabled`（开机自启）。
5. **弹幕落点**：主机 `/usr/share/hassio/share/danmu/danmu_<房间>/<YYYY-MM-DD>.log` ←→ 容器 `/share` ←→ **Samba 里的 `share` 文件夹**（`\\192.168.1.3\share\danmu\`）。

### ✅ 已验证
- 服务 `active + enabled`，容器 `Up`，连上弹幕服务器「认证成功」。
- **抓取→落盘链路实测通过**：临时连热门直播间 21144080，15s 抓到 10 条真实弹幕，格式 `[时间] 用户名(UID): 内容`，正确落盘。
- **崩溃自愈实测**：`docker kill` 后 systemd 10s 内自动拉起。
- 房间 5540720 当前**未开播**（live_status=0），所以暂无弹幕落盘——**这是正常的**，主播开播有人发言就会自动写入。

### 运维备忘
- 看状态：`ssh root@192.168.1.3 'systemctl status danmu; docker logs danmu_5540720 --tail 20'`
- 看弹幕：Samba `\\192.168.1.3\share\danmu\danmu_5540720\` 或主机 `/usr/share/hassio/share/danmu/`
- 改房间：编辑 `/etc/systemd/system/danmu.service` 里的 `ROOM_ID` + `--name` + `-v` 路径，`daemon-reload && restart`。或复制成多个 `danmu@.service` 模板跑多房间。
- 重建镜像（改了 danmu.py 后）：`cd /opt/danmu && docker build -t danmu:latest . && systemctl restart danmu`
- **电脑代理依赖**：仅"拉/建镜像"时需要电脑中继（`clash-lan-proxy.py` on 192.168.1.4:7898）。**日常采集不需要电脑开着**（b站直连）。
- ⏳ 待办（可选）：修 `systemd-journal-gatewayd`（`apt install systemd-journal-remote && systemctl enable --now systemd-journal-gatewayd.socket`）让 HA 脱离 unsupported 状态；给 danmu.service 配 GITHUB_REPO/TOKEN 可选启用云备份。
